# Analyze project brief

Determine whether the brief is sufficient to create an implementation-ready design. Identify explicit assumptions, missing product decisions, accessibility needs, responsive constraints, and content risks. Do not invent requirements silently. If information is missing, mark the status `needs-input` and make the questions actionable.
