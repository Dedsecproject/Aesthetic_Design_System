# Audit design specification

Review the supplied design specification against the Aesthetic Design System. Check hierarchy, semantic token usage, both themes, prohibited visual treatments, component states, responsive behavior, keyboard and screen-reader behavior, contrast, reduced motion, and loading/empty/error states. Report only evidence-based findings. Use `high`, `medium`, or `low` severity and finish with a verdict of `ready`, `ready after fixes`, or `not ready`.
